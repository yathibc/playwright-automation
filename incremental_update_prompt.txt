Read these files first and use them as the source of truth before making any changes:
- `prompt.txt`
- `screen_flow.md`
- `automation_authoring_prompt.txt`
- `CHANGE_TRACKER.md`

We are continuing the same automation project incrementally.
Do not rewrite the framework from scratch.
Preserve the current TS and Java structure unless a real UI change requires modification.

Now inspect the live UI on `https://shop.royalchallengers.com` in headed mode using Playwright MCP.
Compare the actual UI against:
- the expected flow in `screen_flow.md`
- the current codebase
- the latest notes in `CHANGE_TRACKER.md`

Your task for this run:
1. identify only the newly changed or newly visible UI pieces
2. patch the affected TS files under `Playwright_TS/`
3. patch the affected Java files under `playwright_java_tickets/`
4. preserve session restore, OTP flow, monitoring, retry, screenshot, and keep-browser-open behavior
4a. preserve multi-account / multi-browser behavior and per-account session isolation
4b. preserve planned payment configuration such as `UPI_ID` when updating prompt/spec/code later
5. update `CHANGE_TRACKER.md` with:
   - what changed on the site in this run
   - what files were updated
   - what still remains pending or fragile
6. do not automate payment
7. verify the updated code with appropriate validation/build commands

Rules for this incremental run:
- patch only impacted code
- preserve existing skeleton flow
- preserve per-account session separation and do not collapse back to single-account logic
- do not replace stable modules unnecessarily
- prefer small safe changes over broad rewrites
- explicitly document locator fixes and UI differences
- if payment UI becomes available, use configured `UPI_ID` rather than hardcoding values in implementation

For this run, focus specifically on:
- making selectors more direct and faster in login flow and checkout flow.
- for match/stand/seat selection we can inpect when the ticket are live and ui is available.
- for login flow open browser and go to https://shop.royalchallengers.com. use phone number 8660871166 in existing login scripts.
- after login, click on merchandise and select "RCB 2026 Royalcat Comfort Slides".
- click on add to bag and then click on go to bag
- and then checkout the existing scripts of checkout flow with payment type UPI to make selectors more direct and faster.


At the end, provide:
- what changed on the site
- what files were changed
- what is still pending
- exact commands to run next

Examples for the focus section:
- `Tickets navigation is now visible. Identify the correct locator and wire the flow up to ticket discovery.`
- `C Stand and B Stand UI is now visible. Patch stand detection and selection logic only.`
- `The seat map is now visible as SVG. Inspect it and update seat parsing, consecutive seat logic, and retry-on-occupied-seat handling.`
- `After clicking Next, the occupied-seat failure message changed. Update retry detection accordingly.`
